[project]
name = "example-elastic-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "elasticsearch>=8.17.2",
    "mcp>=1.3.0",
]

[dependency-groups]
dev = [
    "mcp[cli]>=1.3.0",
]
